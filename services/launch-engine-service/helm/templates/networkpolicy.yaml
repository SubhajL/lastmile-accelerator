apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: launch-engine-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: launch-engine-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
