name: ci-launch-engine-service
on:
  push: { branches: [ main ], paths: [ 'services/launch-engine-service/**' ] }
jobs:
  call:
    uses: ./.github/workflows/_service-ci.yml
    with:
      lang: node
      context: services/launch-engine-service
      dockerfile: services/launch-engine-service/Dockerfile
      image_suffix: launch-engine-service
