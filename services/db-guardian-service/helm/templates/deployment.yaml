apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-guardian-service
spec:
  replicas: 1
  selector:
    matchLabels: { app: db-guardian-service }
  template:
    metadata:
      labels: { app: db-guardian-service }
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: db-guardian-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          env:
          {{- range $name, $val := .Values.env }}
            - name: {{ $name }}
              value: {{ $val | quote }}
          {{- end }}
          {{- if .Values.externalSecrets.enabled }}
          envFrom:
            - secretRef:
                name: {{ .Values.externalSecrets.targetName }}
          {{- end }}
          ports:
            - containerPort: 7105
          {{- if .Values.grpc.enabled }}
            - containerPort: {{ .Values.grpc.port }}
              name: grpc
          {{- end }}
          readinessProbe:
            httpGet: { path: /healthz, port: 7105 }
            initialDelaySeconds: 2
          livenessProbe:
            httpGet: { path: /healthz, port: 7105 }
            initialDelaySeconds: 10
          resources:
            requests:
              cpu: "{{ .Values.resources.requests.cpu }}"
              memory: "{{ .Values.resources.requests.memory }}"
            limits:
              cpu: "{{ .Values.resources.limits.cpu }}"
              memory: "{{ .Values.resources.limits.memory }}"
