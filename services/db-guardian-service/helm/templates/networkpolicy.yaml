apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: db-guardian-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: db-guardian-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
