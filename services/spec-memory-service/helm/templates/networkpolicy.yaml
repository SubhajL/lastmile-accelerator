apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: spec-memory-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: spec-memory-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
