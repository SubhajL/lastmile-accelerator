apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: scm-app-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: scm-app-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
