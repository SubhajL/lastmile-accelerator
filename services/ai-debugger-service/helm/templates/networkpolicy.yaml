apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ai-debugger-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: ai-debugger-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
