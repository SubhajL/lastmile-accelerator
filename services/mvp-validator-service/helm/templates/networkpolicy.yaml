apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mvp-validator-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: mvp-validator-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
