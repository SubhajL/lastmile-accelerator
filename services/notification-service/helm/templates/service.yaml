apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  type: ClusterIP
  selector: { app: notification-service }
  ports:
    - name: http
      port: 7902
      targetPort: 7902
