apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: zip-ingest-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: zip-ingest-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
