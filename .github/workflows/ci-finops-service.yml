name: ci-finops-service
on:
  push: { branches: [ main ], paths: [ 'services/finops-service/**' ] }
jobs:
  call:
    uses: ./.github/workflows/_service-ci.yml
    with:
      lang: python
      context: services/finops-service
      dockerfile: services/finops-service/Dockerfile
      image_suffix: finops-service
