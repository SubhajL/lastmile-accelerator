apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: webhook-relay-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: webhook-relay-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
