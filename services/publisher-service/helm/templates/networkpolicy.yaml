apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: publisher-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: publisher-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
