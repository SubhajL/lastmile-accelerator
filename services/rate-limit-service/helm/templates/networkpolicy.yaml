apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rate-limit-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: rate-limit-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
