apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: secrets-env-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: secrets-env-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
