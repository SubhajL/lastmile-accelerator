apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: fix-engine-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: fix-engine-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
