name: ci-positioning-engine-service
on:
  push: { branches: [ main ], paths: [ 'services/positioning-engine-service/**' ] }
jobs:
  call:
    uses: ./.github/workflows/_service-ci.yml
    with:
      lang: python
      context: services/positioning-engine-service
      dockerfile: services/positioning-engine-service/Dockerfile
      image_suffix: positioning-engine-service
