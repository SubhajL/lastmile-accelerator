name: ci-db-guardian-service
on:
  push: { branches: [ main ], paths: [ 'services/db-guardian-service/**' ] }
  pull_request: { paths: [ 'services/db-guardian-service/**' ] }
jobs:
  call:
    if: github.event_name == 'push'
    uses: ./.github/workflows/_service-ci.yml
    with:
      lang: go
      context: services/db-guardian-service
      dockerfile: services/db-guardian-service/Dockerfile
      image_suffix: db-guardian-service
      publish: true
    permissions:
      contents: read
      packages: write
      id-token: write

  pr-ci:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-22.04
    permissions:
      contents: read
    defaults:
      run:
        working-directory: services/db-guardian-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: '1.24' }
      - name: Unit tests (go)
        run: go test ./... -cover
