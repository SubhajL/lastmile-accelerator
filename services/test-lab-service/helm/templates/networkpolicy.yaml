apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test-lab-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: test-lab-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
