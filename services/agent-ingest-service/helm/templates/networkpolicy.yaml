apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: agent-ingest-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: agent-ingest-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
