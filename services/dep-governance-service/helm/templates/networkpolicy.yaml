apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dep-governance-service-deny-all
spec:
  podSelector:
    matchLabels:
      app: dep-governance-service
  policyTypes: ["Ingress","Egress"]
  ingress: [{}]
  egress: [{}]
